# Replace placeholders before rendering/deploying. No credentials belong here.
# Supply image.reference via --set-string using a verified release digest.
ingress:
  className: alb
  host: "" # Catch-all rule allows the AWS-generated ALB hostname.
  publicSubnets:
    - REPLACE_WITH_PUBLIC_SUBNET_A
    - REPLACE_WITH_PUBLIC_SUBNET_B
imagePullSecrets: []
