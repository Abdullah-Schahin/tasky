apiVersion: v1
kind: Service
metadata:
  name: tasky
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tasky
  ports:
    - name: http
      port: 80
      targetPort: http
