apiVersion: v1
kind: ServiceAccount
metadata:
  name: tasky
  namespace: {{ .Release.Namespace }}
# DELIBERATE: expose the workload's Kubernetes API credentials to the pod.
automountServiceAccountToken: true
